sdfRender.h:
 - Лучше классы раскидаать по разным h-ам.
 - Можно реализацию pictureWidth() и pictureHeight() вынести в cpp.
 - К некоторым именам можно применить CamelCase.

sdfRender.cpp:
 - В draw_text() обработку строки можно вынести в отдельный метод.
 - В path_draw 
   while (d_cont[j] != ' ')
   {
   	s1.append(d_cont[j]);
	++j;
   }
   можно вынести в отдельный метод.
 - У if-ов разные стили (фигурные скобки раставлены по-разному).
 - В некоторых местах у "==" не стоят пробелы.
